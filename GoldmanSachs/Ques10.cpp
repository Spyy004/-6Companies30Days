Solution:
Create a min heap of size 100. Insert the initial 100 elements. Now keep on comparing the elements in array with the smallest element in the heap.
  If the current element is greater than the smallest element then insert it in the heap and remove the top element. 
